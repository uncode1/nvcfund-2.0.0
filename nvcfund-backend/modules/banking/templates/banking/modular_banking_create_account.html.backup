<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modular Banking Create Account - NVC Banking Platform</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            background: linear-gradient(135deg, #0a2447 0%, #1e3a5f 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
        }

        /* Professional Navbar */
        .navbar {
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%) !important;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
            padding: 0.75rem 0;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.3rem;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
            color: white !important;
        }

        .navbar-nav .nav-link {
            color: rgba(255, 255, 255, 0.9) !important;
            font-weight: 500;
            transition: all 0.3s ease;
            border-radius: 6px;
            margin: 0 2px;
            padding: 0.5rem 0.8rem !important;
        }

        .navbar-nav .nav-link:hover {
            background-color: rgba(255, 255, 255, 0.1);
            color: white !important;
            transform: translateY(-1px);
        }

        .btn-light {
            background-color: white;
            color: #1e3c72;
            font-weight: 500;
        }

        /* Form Container */
        .form-container {
            max-width: 800px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .form-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .form-header {
            background: linear-gradient(135deg, #0a2447 0%, #1a365d 100%);
            color: white;
            padding: 2rem;
            text-align: center;
        }

        .form-header h3 {
            margin: 0 0 0.5rem 0;
            font-weight: 600;
        }

        .form-header p {
            margin: 0;
            opacity: 0.9;
            font-size: 0.95rem;
        }

        .form-body {
            padding: 2rem;
        }

        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 8px;
            padding: 12px 15px;
            margin-bottom: 1rem;
            font-size: 0.95rem;
        }

        .form-control:focus {
            border-color: #0a2447;
            box-shadow: 0 0 0 0.2rem rgba(10, 36, 71, 0.25);
        }

        .form-label {
            font-weight: 500;
            color: #333;
            margin-bottom: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, #0a2447 0%, #1e3a5f 100%);
            border: none;
            border-radius: 8px;
            padding: 12px 30px;
            font-weight: 600;
            width: 100%;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        .alert {
            border-radius: 8px;
            margin-bottom: 1rem;
        }

        .form-nav-links {
            text-align: center;
            padding-top: 1rem;
            border-top: 1px solid #e9ecef;
            margin-top: 1.5rem;
        }

        .form-nav-links a {
            color: #0a2447;
            text-decoration: none;
            font-weight: 500;
        }

        .form-nav-links a:hover {
            color: #1e3a5f;
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .form-container {
                margin: 1rem auto;
                padding: 0 0.5rem;
            }
            .form-header {
                padding: 1.5rem;
            }
            .form-body {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Professional Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container-fluid px-3">
            <a class="navbar-brand fw-bold d-flex align-items-center" href="/">
                <i class="fas fa-university me-2" style="color: #ffd700;"></i>
                <span class="d-none d-sm-inline">NVC Banking Platform</span>
                <span class="d-inline d-sm-none">NVC</span>
            </a>

            <div class="navbar-nav ms-auto d-flex flex-row">
                <a class="nav-link me-3" href="/">
                    <i class="fas fa-home me-1"></i> Home
                </a>
                <a class="nav-link me-3" href="/about">
                    <i class="fas fa-info-circle me-1"></i> About
                </a>
                <a class="nav-link me-3" href="/contact">
                    <i class="fas fa-envelope me-1"></i> Contact
                </a>
                <a class="btn btn-light btn-sm" href="/auth/login" style="border-radius: 20px; padding: 0.375rem 1rem;">
                    <i class="fas fa-sign-in-alt me-1"></i> Sign In
                </a>
            </div>
        </div>
    </nav>

    <!-- Form Content -->
    <div class="form-container">
        <div class="form-card">
            <div class="form-header">
                <h3><i class="fas fa-user-plus me-2"></i>Modular Banking Create Account</h3>
                <p>Complete your banking banking form with enterprise-grade security</p>
            </div>
            
            <div class="form-body">
                
                
                Create New Account


<div class="container-fluid mt-4">
    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card bg-dark border-success">
                <div class="card-header bg-gradient text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h3 class="mb-0">
                                <i class="fas fa-plus-circle me-2"></i>Create New Account
                            </h3>
                            <small class="text-light">Open a new banking account with competitive rates and benefits</small>
                        </div>
                        <a href="{{ url_for('banking.accounts') }}" class="btn btn-outline-light">
                            <i class="fas fa-arrow-left me-2"></i>Back to Accounts
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <!-- Account Creation Form -->
        <div class="col-md-8 mb-4">
            <div class="card bg-dark border-primary">
                <div class="card-header bg-gradient text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-file-alt me-2"></i>Account Application
                    </h5>
                </div>
                <div class="card-body">
                
                <form method="POST" action="{{ url_for('banking.create_account') }}">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="account_type" class="form-label text-light">Account Type</label>
                                    <select class="form-control bg-dark text-light border-secondary" 
                                            id="account_type" name="account_type" required>
                                        <option value="">Select account type</option>
                                        {% for acc_type in account_types %}
                                        <option value="{{ acc_type }}">{{ acc_type }}</option>
                                        {% endfor %}
                                    </select>
                                    <div class="form-text text-muted">Choose the type of account that best suits your needs</div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="currency" class="form-label text-light">Currency</label>
                                    <select class="form-control bg-dark text-light border-secondary" 
                                            id="currency" name="currency">
                                        {% for curr in currencies %}
                                        <option value="{{ curr }}" {{ 'selected' if curr == 'USD' else '' }}>{{ curr }}</option>
                                        {% endfor %}
                                    </select>
                                </div>
                            </div>
                        </div>
                        
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="branch" class="form-label text-light">Preferred Branch</label>
                                    <select class="form-control bg-dark text-light border-secondary" 
                                            id="branch" name="branch">
                                        <option value="Main Branch">Main Branch</option>
                                        <option value="Downtown Branch">Downtown Branch</option>
                                        <option value="Business Center">Business Center</option>
                                        <option value="Online Banking">Online Banking</option>
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="initial_deposit" class="form-label text-light">Initial Deposit (Optional)</label>
                                    <input type="number" class="form-control bg-dark text-light border-secondary" 
                                           id="initial_deposit" name="initial_deposit" step="0.01" min="0"
                                           placeholder="0.00">
                                    <div class="form-text text-muted">Minimum deposit may apply for certain account types</div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="mb-4">
                            <h6 class="text-light mb-3">Terms and Conditions</h6>
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="terms_agreement" required>
                                <label class="form-check-label text-light" for="terms_agreement">
                                    I agree to the <a href="#" class="text-info">Terms and Conditions</a> and 
                                    <a href="#" class="text-info">Account Agreement</a>
                                </label>
                            </div>
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="privacy_policy" required>
                                <label class="form-check-label text-light" for="privacy_policy">
                                    I acknowledge the <a href="#" class="text-info">Privacy Policy</a> and consent to data processing
                                </label>
                            </div>
                        </div>
                        
                        <div class="d-grid">
                            <button type="submit" class="btn btn-success btn-lg">
                                <i class="fas fa-check me-2"></i>Submit Account Application
                            </button>
                        </div>
                    </form>
                
                <div class="form-nav-links">
                    <a href="/auth/login">Already have an account? Sign in</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>